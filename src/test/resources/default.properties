# Configuration file for common properties
# environment value => Loaded from System property. Default value is loaded from pom.xml in the absence of System property
# For Environment specific properties check: src/test/resources/environment/*.properties
environment=${environment}
